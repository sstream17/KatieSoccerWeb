@page "/play"
@page "/play/{id}"

<GameContainer Id="@Id" />

@code {
    [Parameter]
    public string Id { get; set; }

    protected override void OnInitialized()
    {
        if (Id == null)
        {
            Id = Guid.NewGuid().ToString();
        }
    }
}